#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * This function demonstrates the use of printf to print.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
        /* Print a message using printf */
        printf("with proper grammer, but the outcome is a piece of art,");
        return (0);
}
